import React from "react";
import PricingPage from "../pages/PricingPage";

function PricingComponent(props) {
    return (
        <PricingPage settings={props.settings}/>
    )
}

export default PricingComponent